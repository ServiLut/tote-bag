{
  "name": "tote-bag",
  "packageManager": "pnpm@10.28.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "start": "turbo start",
    "install:all": "pnpm install",
    "api": "bruno open api-docs",
    "ngrok:api": "node ./scripts/ngrok-api.mjs",
    "smoke:staging": "node ./scripts/smoke-staging.mjs"
  },
  "devDependencies": {
    "turbo": "^2.8.1"
  }
}
